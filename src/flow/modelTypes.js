// @flow

export type FoodItemData = {
  id: number,
  name: string,
  protein: number,
  fat: number,
  carbohydrate: number,
};

export type DayFoodItemData = {
  item_id: number,
  added_at: string,
  weight: number,
};
